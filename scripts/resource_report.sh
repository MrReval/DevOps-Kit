#!/bin/bash

# Show CPU and RAM usage of containers
docker stats --no-stream

# Implement progress bar for each container
# (This part would need a custom function to calculate usage and display color)
